---
kind: pipeline
type: docker
name: default
clone:
  disable: true
steps:
- name: clone
  image: alpine/git
  commands:
    - git clone https://gitclone.com/github.com/hash-roar/opeplatform.git


- name: greeting
  image: alpine
  commands:
    - echo hello
    - echo world
