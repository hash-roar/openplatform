---
kind: pipeline
type: docker
name: default
clone:
  disable: true
steps:
- name: clone
  image: alpine/git
  commands:
    - git clone git@git.zhlh6.cn:hash-roar/opeplatform.git



- name: greeting
  image: alpine
  commands:
    - echo world
